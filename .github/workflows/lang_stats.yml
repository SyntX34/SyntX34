name: Update Language Stats

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "lang_stats.py"
      - ".github/workflows/lang_stats.yml"

concurrency:
  group: lang-stats
  cancel-in-progress: true

jobs:
  generate-lang-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Generate language stats & lifetime activity SVGs
        run: |
          python lang_stats.py
          python lifetime_activity.py
        env:
          METRICS_TOKEN: ${{ secrets.METRICS_TOKEN || secrets.GH_PAT || secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check for changes
        id: verify_diff
        run: |
          git diff --quiet lang-stats.svg lifetime-activity.svg || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.verify_diff.outputs.changed == 'true'
        run: |
          git config --local user.email "97087641+SyntX34@users.noreply.github.com"
          git config --local user.name "SyntX34"
          git add lang-stats.svg lifetime-activity.svg
          git commit -m "📊 Update language & lifetime activity stats [skip ci]" || exit 0
          git pull --rebase origin main || true
          git push origin HEAD:main
